-- -*- lua -*-


--shell.run("pipe cat startup")
--shell.run("pipe cat usr/startup")
--shell.run("pipe cat startup | cat usr/startup")
--shell.run("pipe cat usr/startup | cat startup")
--shell.run("pipe cat usr/startup | ping srv")
shell.run("pipe cat usr/startup | more")
--shell.run("pipe cat startup | cat")

